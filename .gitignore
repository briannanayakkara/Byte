# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files (never commit secrets)
.env
.env.*
!.env.example

# Raw purchased/downloaded stock assets -- not for redistribution (license
# terms), and not served: only the derived public/character/*.png (built by
# scripts/build-character-assets.mjs) are actually used by the app.
source-assets

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
